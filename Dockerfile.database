# Dockerfile.database

FROM alpine:latest

# Install SQLite
RUN apk --update add sqlite

# Set the working directory
WORKDIR /data

# Expose a volume for persistence
VOLUME /data

# Create an SQLite database file within the volume
RUN sqlite3 mydatabase.db
